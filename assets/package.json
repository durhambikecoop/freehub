{
  "name": "app",
  "private": "true",
  "dependencies": {
    "@hotwired/stimulus": "^3.2.1",
    "@hotwired/turbo-rails": "^7.2.4",
    "esbuild": "^0.17.2"
  },
  "scripts": {
    "js": "esbuild src/index.js --bundle --sourcemap --outdir=../app/assets/builds --public-path=assets",
    "watch:js": "nodemon -e js -x 'npm run js'",
    "watch": "npm run watch:js"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
