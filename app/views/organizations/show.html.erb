<p style="color: green"><%= notice %></p>
<%= link_to "Log out", destroy_user_session_path, data: { "turbo-method": :delete } %>
<%= render @org %>

<div>
  <%= link_to "Edit this organization", edit_organization_path(@org) %> |
  <%= link_to "Back to organizations", organizations_path %>

  <%= button_to "Destroy this organization", @org, method: :delete %>
  <%= render Organizations::PersonCheckInComponent.new(org: @org) %>
</div>
