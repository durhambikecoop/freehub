<%= form_with(model: visit, url: organization_visits_path(@org)) do |form| %>
  <% if visit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(visit.errors.count, 'error') %> prohibited this visit from being saved:</h2>

      <ul>
        <% visit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :note_id, style: "display: block" %>
    <%= form.text_field :note_id %>
  </div>

  <div>
    <%= form.label :volunteer, style: "display: block" %>
    <%= form.check_box :volunteer %>
  </div>

  <div>
    <%= form.label :arrived_at, style: "display: block" %>
    <%= form.datetime_field :arrived_at %>
  </div>

  <div>
    <%= form.label :start_at, style: "display: block" %>
    <%= form.datetime_field :start_at %>
  </div>

  <div>
    <%= form.label :end_at, style: "display: block" %>
    <%= form.datetime_field :end_at %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
